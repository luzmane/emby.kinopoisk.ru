{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach",
            "preLaunchTask": "build, deploy, start server",
            "processName": "EmbyServer",
            //"justMyCode": false
        }
    ]
}
